# bugzilla id : 
# Configuration for RGW zone modification test on primary cluster
config:
  test_ops:
    run_zone_modify_test: true
    new_data_pool: rgwec86-pool
    zone_names: ["secondary"]
    is_multisite: true
    use_sudo: false