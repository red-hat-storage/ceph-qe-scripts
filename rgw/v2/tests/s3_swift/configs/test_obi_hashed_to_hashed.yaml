# Script: ceph-qe-scripts/rgw/v2/tests/s3_swift/test_bucket_ordered_indexing.py
# Sanity: hashed -> hashed traditional reshard (different shard count);
# confirms classic reshard still works alongside OBI.
# polarion-id:
config:
  user_count: 1
  bucket_count: 2
  objects_count: 100
  max_objects_per_shard: 10
  shards: 41
  objects_size_range:
    min: 5
    max: 15
  test_ops:
    hashed_to_hashed: true
    delete_bucket_object: true
