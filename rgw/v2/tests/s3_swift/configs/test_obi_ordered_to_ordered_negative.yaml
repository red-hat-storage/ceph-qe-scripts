# Script: ceph-qe-scripts/rgw/v2/tests/s3_swift/test_bucket_ordered_indexing.py
# Negative: ordered -> ordered reshard must be rejected with a clear error;
# bucket stays usable; ordered->hashed and hashed->ordered still succeed.
# polarion-id:
config:
  user_count: 1
  bucket_count: 1
  objects_count: 50
  max_objects_per_shard: 10
  shards: 20
  objects_size_range:
    min: 5
    max: 15
  test_ops:
    ordered_to_ordered_negative: true
    shards_retry: 48
    objects_count_post_rejected: 20
    delete_bucket_object: true
